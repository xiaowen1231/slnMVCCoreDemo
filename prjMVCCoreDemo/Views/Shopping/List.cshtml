@model IEnumerable<prjMVCCoreDemo.Models.TProduct>

@{
    ViewData["Title"] = "List";
}

<h1>線上選課</h1>

<p>
    <a asp-action="ViewCart">檢視選課單</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                序號
            </th>
            <th>
                @Html.DisplayNameFor(model => model.FName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.FQty)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.FPrice)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.FImagePath)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @{
            int count = 0;
            foreach (var item in Model)
            {
                count++;
                <tr>
                    <td>
                        @count
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.FName)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.FQty)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.FPrice)
                    </td>
                    <td>
                        <img src="~/images/@item.FImagePath" width="100"/>
                    </td>
                    <td>
                        @Html.ActionLink("加入", "AddToCart", new {  id=item.FId  }) 
                    </td>
                </tr>
            }
        }
    </tbody>
</table>
